import Type_Like_GPT as T


def Currency_Converter():
    T.type_text(
        "Welcome To Currency Converter \nAvailable Currency to Exchange with are : USD, INR, CDA, ")
    amount = float(input("Enter Amount : "))
    source = input("Enter ")
